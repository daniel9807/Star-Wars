---
import { type APIStarWars } from "../types/api";
const res = await fetch("https://swapi.py4e.com/api/people", {
  method: "GET",
  headers: {
    "Content-Type": "application/json",
  },
});

const { results } = (await res.json()) as APIStarWars;
---
